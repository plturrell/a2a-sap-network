/* global sap */
sap.ui.define([
    'sap/ui/core/mvc/Controller'
], (Controller) => {
    'use strict';

    return Controller.extend('a2a.network.launchpad.controller.App', {
        onInit: function () {

        }
    });
});
